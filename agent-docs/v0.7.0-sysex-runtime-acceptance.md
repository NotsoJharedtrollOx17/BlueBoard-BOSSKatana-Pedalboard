# v0.7.0 Original KATANA-100 Runtime SysEx Acceptance

## Status

Pending live runtime execution and controlled per-effect restoration checks.
Automated source tests do not fill this record.

The 2026-08-30 target-amplifier session confirmed the official physical v4.00
indicator check and returned six checksum-valid, decoded `effect-states` replies
through input `KATANA 0` and output `KATANA 1`. The six v4.00 reads are therefore
enabled for runtime bootstrap; SysEx writes remain disabled. Controlled
per-effect change/restoration fixtures remain required release evidence.

## Record before testing

- Date/time:
- Amplifier: original BOSS KATANA-100 (Mk I):
- Firmware from the BOSS-prescribed physical version check:
- Windows version:
- BOSS driver version:
- Original BOSS Tone Studio version:
- Exact MIDI input:
- Exact MIDI output:
- USB cable/port:
- Mido/python-rtmidi versions:
- Initial preset and six effect flags:
- Backup/restoration location:

Close Tone Studio and other MIDI clients. Back up important Tone Settings and use
a safe volume. Stop on an unexpected state change, ambiguous port, invalid frame,
or failed restoration.

## Remaining per-effect evidence

For each of Boost, Mod, FX, Delay, Reverb, and Send/Return:

1. Save a baseline `effect-states` fixture.
2. Change exactly that flag using a validated standard control or physical/Tone
   Studio operation, then close the competing MIDI client.
3. Save the changed fixture and verify only the intended/coupled physical behavior.
4. Restore the original state, save a restoration fixture, and verify it physically.
5. Sanitize host paths, addresses, and identifiers; add model, exact firmware,
   physical observation, and restoration metadata to the tracked fixture.
6. Keep `writeAccess="none"`; no runtime SysEx parameter write is permitted.

Current-selection/editor-mode evidence is not a v0.7.0 gate.

## Runtime sequence

With the local profile set to the captured v4.00 firmware and its independently
resolved ports:

```powershell
.\diagnosePedalboard.ps1
.\runPedalboard.ps1 --debug --execute-actions
```

- [ ] Doctor resolves input/output independently and opens neither.
- [ ] Active startup opens input before output and logs six `source=queried` values.
- [ ] Physical/Tone Studio state matches the startup snapshot.
- [ ] Changing state before launch changes the next startup snapshot.
- [ ] C/D use the queried grouped state; both-on test data remains representable.
- [ ] A/B mark prior observations stale, briefly load labeled predictions, and
      replace them with queried values from the newly loaded temporary patch.
- [ ] C/D read the current live group before choosing the opposite CC value and
      read it back afterward; a mismatch fails instead of claiming confirmation.
- [ ] Unplug/replug Katana; a detected failure invalidates state and the next
      operation reopens/resynchronizes without restarting BlueBoard BLE.
- [ ] Forced timeout leaves preset selection available and rejects unknown toggles.
- [ ] A late pre-reconnect reply cannot update the new epoch.
- [ ] Ctrl+C joins the worker and closes both MIDI ports without hanging.
- [ ] Original amplifier state is restored.

## Release decision

- [ ] Six exact-firmware sanitized fixture sets are committed.
- [x] Six v4.00 definitions are approved for read-only production startup access.
- [ ] Full tests, Ruff, scripts, Markdown links, package builds, and installed-wheel
      smoke checks pass.
- [ ] Runtime hardware sequence above passes.
- [ ] Merge to `main` and tag only from the accepted commit.
- Final decision: pending.
